---

---

<section>
    <div class="container-carousel">
        <div class="carruseles" id="slider">
            <section class="slider-section">
                <img src="img/slider/1.avif" />
            </section>
            <section class="slider-section">
                <img src="img/slider/2.avif" />
            </section>
            <section class="slider-section">
                <img src="img/slider/3.avif" />
            </section>
            <section class="slider-section">
                <img src="img/slider/4.jpg" />
            </section>
        </div>
        <div class="content">
            <div class="btn-left">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="128"
                    height="128"
                    viewBox="0 0 24 24"
                    ><path
                        fill="none"
                        stroke="currentColor"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="m16 20l-8-8l8-8"></path></svg
                >
            </div>

            <div class="cuerpo">
                <div class="texto">
                    <span class="primer__span">Dona</span><span class="titulo"> ahora</span>
                    <h1 class="titulo">y Ayudanos a seguir con esta <span class="segundo__span">noble</span> causa</h1>
                    <p>
                        Con tu ayuda serviremos a la iglesia necesitada en todo
                        el mundo
                    </p>
                </div>
                <div class="donar__section">
                    <button>
                        <span>Donar</span>
                    </button>
                    <div class="info__donaciones">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="128"
                            height="128"
                            viewBox="0 0 20 20"
                            ><g
                                fill="currentColor"
                                fill-rule="evenodd"
                                clip-rule="evenodd"
                                ><path
                                    d="M5 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4m0 1a3 3 0 1 0 0-6 3 3 0 0 0 0 6"
                                ></path><path
                                    d="M3.854 8.896a.5.5 0 0 1 0 .708l-.338.337A3.47 3.47 0 0 0 2.5 12.394v1.856a.5.5 0 1 1-1 0v-1.856a4.47 4.47 0 0 1 1.309-3.16l.337-.338a.5.5 0 0 1 .708 0m11.792-.3a.5.5 0 0 0 0 .708l.338.337A3.47 3.47 0 0 1 17 12.094v2.156a.5.5 0 0 0 1 0v-2.156a4.47 4.47 0 0 0-1.309-3.16l-.337-.338a.5.5 0 0 0-.708 0"
                                ></path><path
                                    d="M14 9a2 2 0 1 1 0-4 2 2 0 0 1 0 4m0 1a3 3 0 1 1 0-6 3 3 0 0 1 0 6m-4.5 3.25a2.5 2.5 0 0 0-2.5 2.5v1.3a.5.5 0 0 1-1 0v-1.3a3.5 3.5 0 0 1 7 0v1.3a.5.5 0 1 1-1 0v-1.3a2.5 2.5 0 0 0-2.5-2.5"
                                ></path><path
                                    d="M9.5 11.75a2 2 0 1 0 0-4 2 2 0 0 0 0 4m0 1a3 3 0 1 0 0-6 3 3 0 0 0 0 6"
                                ></path></g
                            ></svg
                        >
                        <div class="text__info__donaciones">
                            <span>60,000</span>
                            <span>Contribuyentes</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="btn-right">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="128"
                    height="128"
                    viewBox="0 0 24 24"
                    ><path
                        fill="none"
                        stroke="currentColor"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="m8 20 8-8-8-8"></path></svg
                >
            </div>
        </div>
    </div>
</section>

<script is:inline>
    // TAMAÑOS PARA EL SLIDER DINAMICO
    const tamanoCarruseles = 400;
    const tamanoSlider = 4;

    document.documentElement.style.setProperty(
        "--tamano-carruseles",
        `${tamanoCarruseles}%`,
    );
    document.documentElement.style.setProperty(
        "--tamano-slider",
        tamanoSlider.toString(),
    );

    // FUNCIONALIDAD DEL SLIDER DINAMICO
    const btnLeft = document.querySelector(".btn-left"),
        btnRight = document.querySelector(".btn-right"),
        slider = document.querySelector("#slider"),
        sliderSection = document.querySelectorAll(".slider-section");

    btnLeft?.addEventListener("click", (e) => moveToLeft());

    btnRight?.addEventListener("click", (e) => moveToRight());

    setInterval(() => {
        moveToRight();
    }, 5000);

    let operacion = 0,
        counter = 0,
        widthImg = 100 / sliderSection.length;

    function moveToRight() {
        if (counter >= sliderSection.length - 1) {
            counter = 0;
            operacion = 0;
            slider.style.transform = `translate(-${operacion}%)`;
            slider.style.transition = "all ease .6s";

            return;
        }
        counter++;
        operacion = operacion + widthImg;
        slider.style.transform = `translate(-${operacion}%)`;
        slider.style.transition = "all ease .6s";
    }

    function moveToLeft() {
        counter--;
        if (counter < 0) {
            counter = sliderSection.length - 1;
            operacion = widthImg * (sliderSection.length - 1);
            slider.style.transform = `translate(-${operacion}%)`;
            slider.style.transition = "all ease .6s";
            return;
        }
        operacion = operacion - widthImg;
        slider.style.transform = `translate(-${operacion}%)`;
        slider.style.transition = "all ease .6s";
    }
</script>

<style>
    :root {
        --tamano-carruseles;
        --tamano-slider;

        --color-galleta: #ff823ed8;
    }

    .container-carousel {
        position: relative;
        width: 100%;
        height: 100vh;
        overflow: hidden;
    }

    .carruseles {
        width: var(--tamano-carruseles);
        height: 100%;
        display: flex;
    }

    .slider-section {
        position: relative;
        width: calc(100% / var(--tamano-slider));
        height: 100%;
    }

    .slider-section img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        /* filter: brightness(45%); */
    }

    .slider-section::after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            to top,
            rgba(0, 0, 0, 0.95),
            rgba(0, 0, 0, 0.3)
        );
        pointer-events: none;
    }

    .content {
        display: flex;
        align-items: center;
        width: 100%;
        height: 100vh;
        position: absolute;
        top: 50%;
        background-color: transparent;
        transform: translate(0, -50%);
        transition: 0.5s ease;
        user-select: none;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
    }

    .btn-left,
    .btn-right {
        display: flex;
        height: 80px;
        font-size: 1.5rem;
        background-color: transparent;
        font-weight: 600;
        cursor: pointer;
        color: #ffffff81;
        transition: 0.5s ease;
    }

    .btn-left svg,
    .btn-right svg {
        width: 100%;
        height: 100%;
        transition: 0.5s ease;
    }

    .btn-left svg:hover,
    .btn-right svg:hover {
        scale: 1.2;
        color: #fff;
    }

    .btn-left {
        margin-right: calc(12% - 65px);
    }

    .cuerpo {
        display: flex;
        flex-direction: column;
        height: 100%;
        width: 100%;
        background-color: transparent;
        transition: 0.5s ease;
        justify-content: center;
        align-items: left;
    }

    .texto {
        width: 70%;
    }

    .donar__section {
        display: flex;
        justify-content: left;
        padding-top: 20px;
        height: 80px;
        gap: 10px;
    }

    .info__donaciones {
        display: flex;
        align-items: center;
        justify-content: left;
        height: 100%;
        gap: 10px;
    }

    .info__donaciones svg {
        width: 80px;
        height: 80px;
        color: rgba(255, 255, 255, 0.8);
    }

    .text__info__donaciones {
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 100%;
    }

    .text__info__donaciones span {
        color: rgba(255, 255, 255, 0.8);
        text-transform: uppercase;
        font-size: clamp(0.5em, 4vw + 1rem, 1em);
        font-weight: 900;
    }

    .text__info__donaciones span:first-child {
        font-size: clamp(1em, 4vw + 1rem, 2em);
        color: var(--color-galleta);
    }

    .texto .primer__span {
        color: var(--color-galleta);
        text-transform: uppercase;
        font-size: clamp(2.5em, 4vw + 1rem, 3.5em);
        font-weight: 900;
    }

    .texto h1 span.segundo__span {
        color: var(--color-galleta);
    }

    .titulo {
        color: rgba(255, 255, 255, 0.8);
        text-transform: uppercase;
        font-size: clamp(2.5em, 4vw + 1rem, 3.5em);
        font-weight: 900;
    }

    p {
        color: rgba(255, 255, 255, 0.8);
        font-size: clamp(1em, 4vw + 1rem, 1.3em);
        left: 150px;
        font-weight: 400;
    }

    button {
        display: inline-block;
        border-radius: 3px;
        background-color: var(--primary-color);
        border: none;
        color: #ffffff;
        text-align: center;
        font-size: 20px;
        padding: 8px;
        width: 120px;
        height: 100%;
        transition: all 0.5s;
        cursor: pointer;
    }

    button span {
        cursor: pointer;
        display: inline-block;
        position: relative;
        transition: 0.5s;
    }

    button span:after {
        content: "»";
        position: absolute;
        opacity: 0;
        top: 0;
        right: -15px;
        transition: 0.5s;
    }

    button:hover {
        background-color: var(--primary-color-dark);
    }

    button:hover span {
        padding-right: 15px;
    }

    button:hover span:after {
        opacity: 1;
        right: 0;
    }

    @media (max-width: 1150px) {
        .content {
            padding-top: 75px;
        }
    }

    @media (max-width: 650px) {
        .content {
            padding-top: 75px;
        }

        .texto {
            width: 100%;
        }

        .cuerpo {
            padding: 0 5%;
            padding-bottom: 40px;
        }

        .btn-left,
        .btn-right {
            display: none;
        }

        .donar__section {
            flex-direction: column;
            padding-top: 0;
            gap: 0;
        }

        button {
            order: 2;
        }
    }

    @keyframes slideInLeft {
        0% {
            opacity: 0;
            transform: translateX(-50px);
        }
        100% {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .cuerpo {
        opacity: 0;
        animation: slideInLeft 0.8s ease-out forwards;
    }
</style>
